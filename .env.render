# =================================================================
# COMPREHENSIVE .ENV TEMPLATE FOR RENDER DEPLOYMENT
#
# This file contains all variables needed for a successful build and
# production deployment on Render.
#
# CRITICAL: Replace all placeholder values (e.g., "YOUR_SECRET_KEY")
# with your actual credentials in the Render Dashboard.
# =================================================================

# ===== 1. BUILD SAFEGUARD & APP CONFIG (Required for Build Success) =====
# These variables must be set in the Render Dashboard to pass the build safeguard.
VITE_APP_TITLE="Scale Breakers — Designer Toys & Murals"
VITE_APP_LOGO="/logo.png"
VITE_ANALYTICS_ENDPOINT=""
VITE_ANALYTICS_WEBSITE_ID=""

# ===== 2. DATABASE CONFIG (PostgreSQL - Recommended for Codebase Consistency) =====
# The codebase is configured for PostgreSQL. Use a Render PostgreSQL instance.
# The connection string is provided by Render after creating the database.
DATABASE_URL="postgresql://user:password@host:port/database_name"

# ===== 3. STRIPE CONFIG (E-commerce) =====
# Use your LIVE keys for production.
STRIPE_SECRET_KEY="sk_live_YOUR_STRIPE_SECRET_KEY"
VITE_STRIPE_PUBLISHABLE_KEY="pk_live_YOUR_STRIPE_PUBLISHABLE_KEY"

# ===== 4. EMAIL CONFIG (Contact Forms) =====
# Use your preferred SMTP provider (e.g., SendGrid, Gmail App Password).
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER="contact.scalebreakers@gmail.com"
SMTP_PASS="YOUR_16_CHAR_GMAIL_APP_PASSWORD"

# ===== 5. IMAGE UPLOADS (Cloudinary - Recommended Optimization) =====
# The codebase currently uses AWS S3, but Cloudinary is recommended for simplicity.
# If you use AWS S3, uncomment the AWS variables and comment out Cloudinary.
# If you use Cloudinary, you must update the server-side code to use the Cloudinary SDK.

# Option A: AWS S3 (Current Codebase)
# AWS_ACCESS_KEY_ID="YOUR_AWS_ACCESS_KEY_ID"
# AWS_SECRET_ACCESS_KEY="YOUR_AWS_SECRET_ACCESS_KEY"
# AWS_REGION="ap-southeast-2"
# AWS_BUCKET_NAME="scalebreakers-images"

# Option B: Cloudinary (Recommended Optimization)
CLOUDINARY_CLOUD_NAME="YOUR_CLOUDINARY_CLOUD_NAME"
CLOUDINARY_API_KEY="YOUR_CLOUDINARY_API_KEY"
CLOUDINARY_API_SECRET="YOUR_CLOUDINARY_API_SECRET"

# ===== 6. SECURITY & APP SECRETS =====
# Generate a long, random string for JWT signing.
JWT_SECRET="GENERATE_A_LONG_RANDOM_STRING_HERE"

# ===== 7. RENDER SPECIFIC (Optional but good practice) =====
# NODE_ENV should be set to 'production' on Render.
NODE_ENV=production
# The port Render exposes the service on.
PORT=10000
