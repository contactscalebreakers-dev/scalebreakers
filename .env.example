# Scale Breakers - Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# DATABASE
# ============================================
# For MySQL (Local Development - Recommended):
DATABASE_URL=mysql://root:password@localhost:3306/scalebreakers

# For PostgreSQL (Production - Render/Supabase):
# DATABASE_URL=postgresql://user:password@host:5432/scalebreakers

# ============================================
# APPLICATION
# ============================================
NODE_ENV=development
PORT=3000
VITE_APP_TITLE="Scale Breakers — Designer Toys & Murals"
VITE_APP_LOGO="/logo.png"

# ============================================
# AUTHENTICATION
# ============================================
JWT_SECRET=change-this-to-random-32-char-string-in-production

# ============================================
# STRIPE PAYMENTS (Optional - Remove if not selling)
# ============================================
# Get keys from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_test_key_here
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_test_key_here

# ============================================
# EMAIL SERVICE
# ============================================
# Option 1: Gmail SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Option 2: SendGrid
# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=your-sendgrid-api-key

# Email addresses
EMAIL_FROM=contact@scalebreakers.space
EMAIL_TO=daniel@scalebreakers.space

# ============================================
# AWS S3 (Optional - For image uploads)
# ============================================
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key
AWS_REGION=ap-southeast-2
AWS_S3_BUCKET=scalebreakers

# ============================================
# ANALYTICS (Optional)
# ============================================
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=

# ============================================
# NOTES
# ============================================
# 1. Never commit .env to Git (it's in .gitignore)
# 2. Use .env for development
# 3. Use .env.production.example as template for production
# 4. Database URL format:
#    MySQL: mysql://user:pass@host:3306/database
#    PostgreSQL: postgresql://user:pass@host:5432/database
# 5. For Gmail SMTP, you need to:
#    - Enable 2FA on your Google account
#    - Generate an "App Password"
#    - Use the app password in SMTP_PASS
# 6. JWT_SECRET should be a random 32+ character string
#    Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
